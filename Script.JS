/*fetch('db.json').then((response) => {
    response.json().then((contatos) => {
        contatos =>
            `<p><strong>Nome:</strong> ${contatos.nome}<br><strong>Email:</strong> ${contatos.email}</p>`
                .join('');
    })
}
)   
let nome = document.getElementById('#name')*/
